{:config-in-ns
 {anthropic.beta
  {:linters
   {;; The deployment event's result shape is intentionally retained pending API review.
    :unused-value {:level :off}}}

  anthropic.beta.messages
  {:linters
   {;; SDK conversion helpers are retained for feature parity while their dispatch is finalized.
    :unused-private-var {:level :off}
    ;; The clear-thinking API has only an :all branch; other values need API-design review.
    :missing-else-branch {:level :off}}}

  anthropic.core
  {:linters
   {;; SDK conversion helpers are retained for feature parity while their dispatch is finalized.
    :unused-private-var {:level :off}}}

  anthropic.beta-messages-test
  {:linters
   {;; Existing test fixtures retain their bindings for readability; test code is out of scope.
    :redundant-let {:level :off}}}

  anthropic.beta-test
  {:linters
   {;; Existing test imports and fixture definitions are out of scope for this hardening pass.
    :unused-import {:level :off}
    :redefined-var {:level :off}
    :inline-def {:level :off}}}

  anthropic.core-test
  {:linters
   {;; Existing test imports are out of scope for this hardening pass.
    :unused-import {:level :off}}}}}
